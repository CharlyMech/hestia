flutter_native_splash:
  ios: true
  android: false
  web: false
  color: "#007CCC" # fallback flat color

  image: assets/images/splash_logo.png
  ios_content_mode: center

  # --- Dark mode (mirrors light) ---
  color_dark: "#007CCC"
  image_dark: assets/images/splash_logo.png
